{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Move WASM",
            "type": "shell",
            "command": "cargo build --release --target wasm32-wasi && mv ./target/wasm32-wasi/release/llama_edge_agent.wasm ./ && mv ./target/wasm32-wasi/release/agent.wasm ./",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}